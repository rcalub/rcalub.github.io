
// NOTE: Each cubie referenced as its names of faces in clockwise order (lowercase)
// i.e. urf: upper, right, front cubicle (where u is the 0-th face)
//      uf: upper, front cubicle (u is the 0-th face)

class FasterCubie {
    constructor(name, )
}

class FasterCube {
    constructor(eIdx, cIdx) {
        this.eIdx = eIdx; // list with of each edge cubie
        this.cIdx = cIdx; // list with of each corner cubie
    }
}